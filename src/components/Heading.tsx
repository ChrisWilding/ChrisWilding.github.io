import styled from "styled-components"

import { spacingUnit } from "../constants"

const Heading = styled.h2`
  font-size: ${3 * spacingUnit}px;
  padding: ${3 * spacingUnit}px;
  text-align: center;
`

export default Heading
